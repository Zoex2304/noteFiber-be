Issue Description: Dropdown Items Are Visible but Not Selectable
The dropdown list is already visible and successfully populated with data, but there is a critical interaction issue:
The items cannot be clicked using the mouse.
The items cannot be selected using the keyboard (arrow keys).
Although multiple countries and provinces are displayed, none of them become active or selected when interacted with.
From a user perspective, it feels like:
The dropdown is visually working,
But the selection layer is not responding to any input.