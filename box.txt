I want to add a new atomic status pill component in the top bar. This pill must reflect the user’s current subscription plan in real time.
If the user is on the Free Plan, then:
The Search icon must never be visible.
The Chat icon must never be visible.
In the Settings page, there must also be clear information showing which plan the user is currently on, and the same status pill component must be reused there.
In short, the plan status pill must be a reusable atomic component, used consistently in both the top bar and the settings page, and it must dynamically reflect the user’s actual subscription status at all times.